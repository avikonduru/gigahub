import React, { Fragment } from 'react';
import PropTypes from 'prop-types';

const LoginPage = (props) => {
	return <Fragment>Log In Page</Fragment>;
};

LoginPage.propTypes = {};

export default LoginPage;
